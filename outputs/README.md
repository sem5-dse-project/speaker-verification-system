# Experiment outputs (local only — do not commit)

Evaluation reports, plots, score files, and logs belong here:

```text
outputs/
  eval/
  plots/
  logs/
```

Do not commit large generated artifacts. Summaries for the report can be copied into `docs/` selectively if needed.
