# Example only — DO NOT apply this file via Kustomize/ArgoCD.
# Create the live secret once on the cluster (see deploy/k3s/bootstrap.sh).
apiVersion: v1
kind: Secret
metadata:
  name: voice-auth-secrets
  namespace: voice-auth
type: Opaque
stringData:
  DATABASE_HOST: "postgres"
  DATABASE_PORT: "5432"
  DATABASE_USER: "voice_auth"
  DATABASE_PASSWORD: "change-me-strong-password"
  DATABASE_NAME: "voice_authentication"
  JWT_SECRET: "change-me-to-a-long-random-string"
