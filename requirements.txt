# Core runtime (Python 3.11)
numpy>=1.26
torch>=2.1
PyYAML>=6.0

# Development / quality
pytest>=7.4
ruff>=0.4
black>=24.0

# Optional audio I/O (install when implementing real loaders)
# soundfile>=0.12
# librosa>=0.10
