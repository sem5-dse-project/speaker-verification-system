# Production ML deps (no webrtc_noise_gain — often fails to build; use ENHANCEMENT_MODE=none|waveunet)
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
numpy>=1.26
soundfile>=0.12
speechbrain>=1.0.0
huggingface_hub>=0.20
transformers>=4.40
pydantic>=2.0
silero-vad>=5.1
onnxruntime>=1.18
# Optional enhancement (large); comment out for lighter image if unused
denoisers
